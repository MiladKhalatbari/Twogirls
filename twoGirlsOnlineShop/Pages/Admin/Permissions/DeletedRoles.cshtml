@page
@model twoGirlsOnlineShop.Pages.Admin.Permissions.DeletedRolesModel

@{
    ViewData["Title"] = "List of Deleted Roles";
}
@{
    int i = 1;
}
<br />
<div class="table-responsive">

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th class="text-center">
                    #
                </th>
                <th class="text-center">
                    Role Title
                </th>
                <th class="text-center">
                    Recover
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Roles)
            {
                <tr>
                    <th class="text-center">
                        @i
                    </th>
                    <td class="text-center">
                        @item.RoleTitle
                    </td>
                    
                    <td class="text-center">
                        <a class="btn btn-success" href="/admin/Permissions/Recover?id=@item.RoleId">Recover <i class="fa-solid fa-recycle"></i></a>
                    </td>
                </tr>
                i++;
            }
        </tbody>
    </table>
</div>

@section AdminSearchBox {
    <i class="fa fa-search search-show"></i>
    <form class=" search-area ">
        <button type="submit" class="bg-transparent border-0"> <i class="fa fa-search"></i></button>
        <input class="h-50" type="text" placeholder="Search..." name="filter" id="">
        <a href="/admin/users"><i class="fa fa-close"></i></a>
    </form>

}